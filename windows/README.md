# AWG Panel for Windows

Desktop client for the AWG Panel web interface. The application uses Microsoft WebView2 and can be published as a self-contained Windows x64 executable.

GitHub Actions builds `AWG-Panel-Windows-x64` automatically when files under `windows/` change or when manually dispatched.
